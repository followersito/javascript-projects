// Arreglo de objetos
const autos = [
    {modelo: 'Mustang', motor: 6.0},
    {modelo: 'Golf', motor: 2.5},
    {modelo: 'Mazda 3', motor: 2.0},
]

for(let i = 0; i < autos.length; i++) {
    console.log(`${autos[i].modelo} ${autos[i].motor}`);
}
